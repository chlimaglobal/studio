# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  maxInstances: 1
  bundleWebApp: false  # ← DESATIVA App Hosting SSR bugado
  
build:
  command: npm run build  # ← Usa seu next.config.mjs com ignoreBuildErrors

runtime:
  type: static  # ← FORÇA Static Hosting (SAI do bug)
